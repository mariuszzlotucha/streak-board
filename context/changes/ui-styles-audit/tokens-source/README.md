# Źródło tokenów: preset shadcn `b7Br7G9Kq`

Surowe wartości wygenerowane 2026-09-25 poleceniem (uruchomionym w katalogu tymczasowym poza repozytorium, NIE w tym repo):

    npx shadcn@latest init --preset b7Br7G9Kq --base radix --template astro --name preset-probe --yes --no-monorepo

- `preset-b7Br7G9Kq.global.css` — `:root`, `.dark`, `@theme inline`, `@layer base` z presetu (styl `radix-maia`, `baseColor: mist`, font Outfit Variable).
- `preset-b7Br7G9Kq.components.json` — konfiguracja shadcn z presetu.

To jest kopia referencyjna do przeniesienia do `src/styles/global.css` (faza 1 planu); nie jest importowana przez aplikację.
Nie uruchamiaj `shadcn init` w tym repozytorium (repo ma już system projektowy).
